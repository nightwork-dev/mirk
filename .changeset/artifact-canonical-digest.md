---
"@mirk/artifact": patch
---

`finalizationDigest` is deprecated in favor of `artifactFinalizationDigest`, and the metadata fingerprint and finalization digest now use `canonicalDigest` from `@mirk/store`, with identical output.
