# Handoff — Python port of Mirk, 2026-09-02 (updated after phase 2 wave 0)

For whoever resumes (likely Garnet in a later session). Everything below is
committed on `main`; nothing is pushed. Read this, then
`docs/python-port-spec.md`, then `docs/python-port/plan-phase2.md`.

## State at pause

Eight commits ahead of `origin/main`, working tree clean:

| Commit | What |
| --- | --- |
| `55edfb7` | phase 1 wave 0: corpus framework, Python store scaffold (KV/collection) |
| `52d2d2c` | phase 1 wave 1: full phase 1 corpus, Python vector/search/graph, TS parity fixes |
| `5aa4088` | phase 1 wave 2: review fixes, receipts, CI, evidence, MR-20/21/22 rows |
| `36f0c9b` | MR-22 vec0 deletion, MR-21 table registry, phase 2 plan + digests + review |
| `81a55db`…`443eb1e` | atomic limits per backend; `@mirk/store` 0.10.0 release |
| `d7b3170` | **phase 2 wave 0**: atomic API + canonical JSON in Python, hashing corpus, A1 TS prep |

Gates at HEAD, all green: `pnpm conformance:current` (250 scenarios),
`pnpm -r typecheck`, `pnpm test` (now serialized: `pnpm -r
--workspace-concurrency=1 test`, because three store tests rebuild `dist`),
`pnpm docs:check`; in `python/store`: `uv run pytest -q` (863 tests, 500conformance runs, 246 per backend, zero skips), `uv run pyright`, `uv run
ruff check .`, `uv run ruff format --check .`.

## What exists

- **Phase 1 done** (see the earlier sections of the spec; evidence
  `docs/evidence/python-port/2026-09-02-phase1-green.md`).
- **Phase 2 wave 0 (S0) done.** `mirk.store.canonical`, `mirk.store.atomic`,
  `getVersioned`/`mutateAtomically` on both Python backends, the `hash` and
  `atomic` conformance ports in both runners, `conformance/store/atomic/`
  (40) and `conformance/artifact/hashing/` (42), injectable
  `versionIdentity`/`version_identity`, two-direction SQLite exchange with
  shared digests. Evidence:
  `docs/evidence/python-port/2026-09-02-atomic-exchange.md`.
- **A1 TypeScript prep done** in `@mirk/artifact` (injectable audit and lease
  ids, code point sorts, `addLineage` order, lease clock fix). A1's scenario
  module (`scripts/scenarios/artifact.ts`) and the fingerprint/finalization
  ops on the `hash` target are NOT done.
- **Cross-lineage review of `d7b3170` done**: `docs/python-port/reviews/2026-09-02-wave0-code-review-luna.md`; all real findings fixed in the follow-up commit with corpus guards (250 scenarios now).

## Open decisions for David

1. **Push main.** Eight commits; nothing outward has happened.
2. **PyPI name.** `mirk` is taken; `mirk-store` + namespace `mirk.store` is in
   use. No publication yet, no Python registry alongside Verdaccio.
3. **MR-22b.** Does `@mirk/store-libsql`'s native vector path execute?

## How to resume phase 2 (wave 1)

Order: **R0 first** (the real transaction: TS `ArtifactCoordinator` +
`StoreArtifactRepository` over SQLite + `FileObjectStore` finalizes one
artifact with injected id and clock; a fresh Python process opens the file
and directory, verifies, reads bytes and lineage — this needs a first cut of
`mirk.artifact` read/verify, so R0 and A2's read path are one brief), then
F1/F2 (fixtures) and A1-scenarios/A2 in parallel, then I2.

Executor briefs follow the wave 0 pattern:

- Spawn with `subagent_type: "port-executor"` (`.claude/agents/port-executor.md`,
  pinned `model: claude-opus-4-6` per David 2026-09-02; the definition is
  loaded at session start, so it needs a fresh session). Sonnet for fully
  mechanical briefs. Do not route executors through pi.
- Executors never commit, never `git stash|checkout|reset`, never write
  `conformance/` (draft with `pnpm --filter @mirk/store conformance:gen --out
  <scratch>`); the strategist regenerates once per wave and commits.
- Python port authors expose `conformance_target(backend, connection)`; the
  runner resolves `mirk.store.<port>` then `mirk.<port>`.
- Fixtures/artifact Python packages: `python/` becomes a uv workspace with
  members `store`, `fixtures`, `artifact` (ruling 8). Nobody has done the
  workspace conversion yet; it is the first step of F2/A2.
- Brief executors to probe before implementing anything a digest asserts
  about a dependency, and to report disagreements rather than bend the port.

## Concurrency lessons (wave 0 added)

- Four agents in one checkout worked with disjoint file ownership stated in
  every brief; zero collisions this wave.
- Root `pnpm test` raced on `dist` (three store tests run `pnpm build`, which
  starts with `rm -rf dist`); serialized now. A single red collection error
  ("Cannot find package '@mirk/store/…'", "Failed to resolve entry") in a
  busy checkout is that race: rerun alone before diagnosing.
- A mid-run message to an executor may arrive after it has finished; check
  its report for the item rather than assuming delivery.

## Where things live

- Spec: `docs/python-port-spec.md`. Plans: `docs/python-port/plan-phase1.md`
  (done), `docs/python-port/plan-phase2.md` (S0 done; rulings section is
  binding). Digests: `docs/python-port/digests/`. Reviews:
  `docs/python-port/reviews/`. Evidence: `docs/evidence/python-port/`.
- Corpus: `conformance/` with `README.md` as the format authority;
  `pnpm conformance:gen` (integrator only), `pnpm conformance:current` (gate).
- Roadmap rows: MR-20, MR-21, MR-22, MR-22b; MR-23 (fixtures Python) and
  MR-24 (artifact Python) are still to be added in I2.
