{
  "id": "fixtures/ref-grammar/malformed-ref-is-rejected",
  "title": "a ref with two colons is not a ref: the whole string is checked",
  "ports": [
    "fixtures"
  ],
  "capabilities": [],
  "steps": [
    {
      "op": "configure",
      "args": [
        {
          "types": [
            {
              "type": "theme",
              "directory": "themes"
            }
          ],
          "sources": [
            {
              "kind": "memory",
              "name": "pack",
              "priority": 0,
              "files": {
                "themes/dark.json": "{\"name\":\"Dark\"}"
              }
            }
          ]
        }
      ]
    },
    {
      "op": "load",
      "args": [
        "theme:a:b"
      ],
      "expect": {
        "throws": "Invalid fixture ref \"theme:a:b\". Expected \"type:id\"."
      }
    }
  ]
}
