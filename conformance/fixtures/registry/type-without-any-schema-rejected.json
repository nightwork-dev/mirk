{
  "id": "fixtures/registry/type-without-any-schema-rejected",
  "title": "a type declaring neither jsonSchema nor schema is rejected at registration",
  "ports": [
    "fixtures"
  ],
  "capabilities": [],
  "steps": [
    {
      "op": "configure",
      "args": [
        {
          "types": [
            {
              "type": "theme",
              "directory": "themes",
              "jsonSchema": null
            }
          ],
          "sources": [
            {
              "kind": "memory",
              "name": "pack",
              "priority": 0,
              "files": {}
            }
          ]
        }
      ],
      "expect": {
        "throws": "Fixture type \"theme\" must declare \"jsonSchema\" or \"schema\"."
      }
    }
  ]
}
